name: CI
on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn release-patch:commit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
